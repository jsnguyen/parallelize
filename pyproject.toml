[project]
name = "parallelize"
version = "0.0.1"
dependencies = [
  "tqdm",
  "multiprocess"
]
requires-python = ">= 3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"